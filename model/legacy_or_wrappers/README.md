# Legacy / Wrapper Area

Folder ini disiapkan untuk menampung wrapper model lama jika dilakukan migrasi bertahap
ke trainer native di `src/training/*`.

Saat ini script model aktif masih berada di `model/` agar kompatibel dengan flow yang sudah berjalan.
